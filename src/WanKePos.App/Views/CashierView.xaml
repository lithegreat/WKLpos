<Page x:Class="WanKePos.App.Views.CashierView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:local="clr-namespace:WanKePos.App.Views"
      xmlns:converters="clr-namespace:WanKePos.App.Converters"
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="1000"
      Title="CashierView">

    <Page.Resources>
        <converters:DecimalToCurrencyConverter x:Key="CurrencyConverter"/>
        <converters:BoolToVisibilityConverter x:Key="BoolToVis"/>
    </Page.Resources>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="350"/>
        </Grid.ColumnDefinitions>

        <!-- Left Column: Categories -->
        <Border Grid.Column="0" Background="{ui:ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Margin="0,0,10,0" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <ui:TextBox Grid.Row="0" PlaceholderText="搜索分类" Margin="0,0,0,10" Icon="{ui:SymbolIcon Symbol=Search24}"/>
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Categories}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <ui:Button Content="{Binding}" Command="{Binding DataContext.SelectCategoryCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}" Margin="0,0,0,5" HorizontalAlignment="Stretch" Height="40" FontSize="14"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- Center Column: Products -->
        <Border Grid.Column="1" Background="{ui:ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Margin="0,0,10,0" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <ui:TextBox Grid.Row="0" x:Name="BarcodeTextBox" Text="{Binding BarcodeInput, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="扫码枪扫码 或 输入条码/商品名" FontSize="20" Height="48" Margin="0,0,0,10" Icon="{ui:SymbolIcon Symbol=BarcodeScanner24}">
                    <ui:TextBox.InputBindings>
                        <KeyBinding Key="Enter" Command="{Binding SearchOrScanCommand}"/>
                    </ui:TextBox.InputBindings>
                </ui:TextBox>
                <ListView Grid.Row="1"
                          ItemsSource="{Binding DisplayProducts}"
                          ScrollViewer.CanContentScroll="True"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          VirtualizingPanel.IsVirtualizing="True"
                          VirtualizingPanel.VirtualizationMode="Recycling"
                          VirtualizingPanel.ScrollUnit="Pixel"
                          Background="Transparent"
                          BorderThickness="0"
                          HorizontalContentAlignment="Stretch">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{ui:ThemeResource ControlFillColorDefaultBrush}"
                                    BorderBrush="{ui:ThemeResource ControlElevationBorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="8"
                                    Padding="12,10"
                                    Margin="0,3">
                                <Border.InputBindings>
                                    <MouseBinding MouseAction="LeftClick"
                                                  Command="{Binding DataContext.AddToCartCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                                  CommandParameter="{Binding}" />
                                </Border.InputBindings>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Name}" FontSize="15" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                            <TextBlock Text="{Binding Barcode, StringFormat='条码: {0}'}" FontSize="12" Foreground="{ui:ThemeResource TextFillColorTertiaryBrush}" Margin="0,0,12,0"/>
                                            <TextBlock Text="{Binding StoreCategory, StringFormat='分类: {0}'}" FontSize="12" Foreground="{ui:ThemeResource TextFillColorTertiaryBrush}" Margin="0,0,12,0"/>
                                            <TextBlock Text="{Binding Stock, StringFormat='库存: {0}'}" FontSize="12" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                                        </StackPanel>
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="15,0">
                                        <TextBlock Text="{Binding RetailPrice, Converter={StaticResource CurrencyConverter}}" FontSize="16" FontWeight="Bold" Foreground="#FF8C00" HorizontalAlignment="Right"/>
                                        <TextBlock Text="{Binding MemberPrice, StringFormat='会员价: ¥{0:F2}'}" FontSize="12" Foreground="LightGreen" HorizontalAlignment="Right"/>
                                    </StackPanel>
                                    <ui:Button Grid.Column="2" Content="+ 加入"
                                               Command="{Binding DataContext.AddToCartCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                               CommandParameter="{Binding}"
                                               Appearance="Primary" Height="36" Padding="12,0" VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>

        <!-- Right Column: Cart -->
        <Border Grid.Column="2" Background="{ui:ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Member Area -->
                <StackPanel Grid.Row="0" Margin="0,0,0,10">
                    <ui:TextBox Text="{Binding MemberPhoneInput, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="输入手机号查找会员" Margin="0,0,0,5">
                        <ui:TextBox.InputBindings>
                            <KeyBinding Key="Enter" Command="{Binding SearchMemberCommand}"/>
                        </ui:TextBox.InputBindings>
                    </ui:TextBox>
                    <Grid Visibility="{Binding IsMemberMode, Converter={StaticResource BoolToVis}}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding CurrentMember.Name, StringFormat='会员: {0}'}" VerticalAlignment="Center" FontWeight="Bold"/>
                        <ui:Button Grid.Column="1" Content="清除" Command="{Binding ClearMemberCommand}" Appearance="Danger"/>
                    </Grid>
                </StackPanel>
                
                <!-- Cart Items -->
                <ListView Grid.Row="1" ItemsSource="{Binding CartItems}" Background="Transparent" BorderThickness="0" Margin="0,0,0,10">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="3*"/>
                                    <ColumnDefinition Width="2*"/>
                                    <ColumnDefinition Width="2*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding ProductName}" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding ActualPrice, Converter={StaticResource CurrencyConverter}}" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <ui:Button Content="-" Command="{Binding DataContext.DecreaseQuantityCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}" Padding="4,2"/>
                                    <TextBlock Text="{Binding Quantity}" Margin="10,0" FontSize="16" VerticalAlignment="Center"/>
                                    <ui:Button Content="+" Command="{Binding DataContext.IncreaseQuantityCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}" Padding="4,2"/>
                                </StackPanel>
                                <TextBlock Grid.Column="2" Text="{Binding Subtotal, Converter={StaticResource CurrencyConverter}}" FontWeight="Bold" Foreground="#FF8C00" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                <ui:Button Grid.Column="3" Icon="{ui:SymbolIcon Symbol=Delete24}" Command="{Binding DataContext.RemoveFromCartCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}" Appearance="Transparent" Margin="5,0,0,0"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                
                <!-- Totals & Payment -->
                <StackPanel Grid.Row="2">
                    <Border BorderThickness="0,1,0,0" BorderBrush="{ui:ThemeResource DividerStrokeColorDefaultBrush}" Margin="0,0,0,10" Padding="0,10,0,0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="小计:" FontSize="14" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding SubTotal, Converter={StaticResource CurrencyConverter}}" FontSize="14" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                            <TextBlock Grid.Row="1" Grid.Column="0" Text="优惠:" FontSize="14" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding DiscountAmount, Converter={StaticResource CurrencyConverter}}" FontSize="14" Foreground="LightGreen"/>
                            <TextBlock Grid.Row="2" Grid.Column="0" Text="应付:" FontSize="24" FontWeight="Bold" Margin="0,10,0,0"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding PayableAmount, Converter={StaticResource CurrencyConverter}}" FontSize="24" FontWeight="Bold" Foreground="#FF8C00" Margin="0,10,0,0"/>
                        </Grid>
                    </Border>
                    <UniformGrid Columns="2" Rows="2">
                        <ui:Button Content="现金结算" Command="{Binding CheckoutCashCommand}" Background="#FF8C00" Foreground="White" Margin="2" Height="48" FontSize="16"/>
                        <ui:Button Content="余额结算" Command="{Binding CheckoutBalanceCommand}" IsEnabled="{Binding IsMemberMode}" Appearance="Primary" Margin="2" Height="48" FontSize="16"/>
                        <ui:Button Content="清空" Command="{Binding ClearCartCommand}" Appearance="Secondary" Margin="2" Height="48" FontSize="16"/>
                    </UniformGrid>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>
