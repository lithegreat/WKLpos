<Page x:Class="WanKePos.App.Views.MemberListView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:converters="clr-namespace:WanKePos.App.Converters"
      Title="会员管理">
    <Page.Resources>
        <converters:DecimalToCurrencyConverter x:Key="DecimalToCurrencyConverter" />
    </Page.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <ui:TextBox PlaceholderText="搜索会员名称、手机或编号..." Width="280" Margin="0,0,8,0" Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}">
                    <ui:TextBox.InputBindings>
                        <KeyBinding Key="Enter" Command="{Binding SearchCommand}" />
                    </ui:TextBox.InputBindings>
                </ui:TextBox>
                <ui:Button Content="搜索" Command="{Binding SearchCommand}" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <ui:Button Content="➕ 新增会员" Command="{Binding AddMemberCommand}" Appearance="Primary" FontWeight="Bold" Margin="0,0,8,0" Padding="14,6"/>
                <ui:Button Content="📂 导入Excel" Command="{Binding ImportFromExcelCommand}" Padding="12,6"/>
            </StackPanel>
        </Grid>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Members}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  AlternatingRowBackground="#1AFFFFFF"
                  EnableRowVirtualization="True"
                  EnableColumnVirtualization="True"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  VirtualizingPanel.ScrollUnit="Pixel"
                  ScrollViewer.CanContentScroll="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="会员卡号" Binding="{Binding MemberNo}" Width="150"/>
                <DataGridTextColumn Header="手机号" Binding="{Binding Phone}" Width="140"/>
                <DataGridTextColumn Header="姓名" Binding="{Binding Name}" Width="120"/>
                <DataGridTextColumn Header="性别" Binding="{Binding Gender}" Width="70"/>
                <DataGridTextColumn Header="积分" Binding="{Binding TotalPoints}" Width="90"/>
                <DataGridTextColumn Header="余额" Binding="{Binding Balance, Converter={StaticResource DecimalToCurrencyConverter}}" Width="100"/>
                <DataGridTextColumn Header="累计消费" Binding="{Binding TotalSpent, Converter={StaticResource DecimalToCurrencyConverter}}" Width="110"/>
                <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="80"/>
                <DataGridTextColumn Header="专属导购" Binding="{Binding GuideName}" Width="120"/>
                <DataGridTemplateColumn Header="操作" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ui:Button Content="🗑️ 注销" Click="DeleteMemberButton_Click" Padding="8,3" FontSize="12"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
