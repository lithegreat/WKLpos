<Page x:Class="WanKePos.App.Views.ProductListView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:converters="clr-namespace:WanKePos.App.Converters"
      Title="商品管理">
    <Page.Resources>
        <converters:DecimalToCurrencyConverter x:Key="DecimalToCurrencyConverter" />
    </Page.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <ui:TextBox PlaceholderText="搜索商品名称或条码..." Width="250" Margin="0,0,10,0" Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}">
                <ui:TextBox.InputBindings>
                    <KeyBinding Key="Enter" Command="{Binding SearchCommand}" />
                </ui:TextBox.InputBindings>
            </ui:TextBox>
            <ComboBox Width="150" Margin="0,0,10,0" ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory}" />
            <ui:Button Content="搜索" Command="{Binding SearchCommand}" Margin="0,0,10,0" />
            <ui:Button Content="导入Excel" Command="{Binding ImportFromExcelCommand}" />
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Products}" AutoGenerateColumns="False" IsReadOnly="True" AlternatingRowBackground="#1AFFFFFF">
            <DataGrid.Columns>
                <DataGridTextColumn Header="条码" Binding="{Binding Barcode}" />
                <DataGridTextColumn Header="名称" Binding="{Binding Name}" />
                <DataGridTextColumn Header="分类" Binding="{Binding StoreCategory}" />
                <DataGridTextColumn Header="零售价" Binding="{Binding RetailPrice, Converter={StaticResource DecimalToCurrencyConverter}}" />
                <DataGridTextColumn Header="会员价" Binding="{Binding MemberPrice, Converter={StaticResource DecimalToCurrencyConverter}}" />
                <DataGridTextColumn Header="库存" Binding="{Binding Stock}" />
                <DataGridTextColumn Header="单位" Binding="{Binding SaleUnit}" />
                <DataGridTextColumn Header="状态" Binding="{Binding ShelfStatus}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
