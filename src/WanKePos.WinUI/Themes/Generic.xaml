<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:WanKePos.WinUI.Controls">

    <!-- WinUI Gallery 规范 PageHeader 样式 -->
    <Style TargetType="controls:PageHeader">
        <Setter Property="Margin" Value="0,0,0,18"/>
        <Setter Property="IsTabStop" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="controls:PageHeader">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Spacing="4">
                            <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                                <FontIcon Glyph="{TemplateBinding Glyph}" 
                                          FontSize="22" 
                                          Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                          Visibility="{TemplateBinding GlyphVisibility}"
                                          VerticalAlignment="Center"/>
                                <TextBlock Text="{TemplateBinding Title}" 
                                           Style="{ThemeResource TitleTextBlockStyle}"
                                           FontWeight="SemiBold"
                                           VerticalAlignment="Center"/>
                                <!-- WinUI Gallery 键帽风格快捷键 Badge -->
                                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        Padding="8,2"
                                        Margin="6,0,0,0"
                                        Visibility="{TemplateBinding BadgeVisibility}"
                                        VerticalAlignment="Center">
                                    <TextBlock Text="{TemplateBinding BadgeText}" 
                                               FontSize="11" 
                                               FontWeight="Normal" 
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                </Border>
                            </StackPanel>
                            <TextBlock Text="{TemplateBinding Description}" 
                                       Style="{ThemeResource BodyTextBlockStyle}"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" 
                                       TextWrapping="Wrap"
                                       Visibility="{TemplateBinding DescriptionVisibility}"/>
                        </StackPanel>

                        <ContentPresenter Grid.Column="1" 
                                          Content="{TemplateBinding Content}" 
                                          ContentTemplate="{TemplateBinding ContentTemplate}"
                                          ContentTransitions="{TemplateBinding ContentTransitions}"
                                          VerticalAlignment="Center" 
                                          HorizontalAlignment="Right"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Fluent 2 品类筛选导航项默认样式 (未选中：带微交互平移与缩放动效) -->
    <Style x:Key="CategoryItemButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{ThemeResource TextFillColorSecondaryBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="CornerRadius" Value="6"/>
        <Setter Property="Height" Value="36"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="Padding" Value="12,0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="RootBorder" 
                            Background="{TemplateBinding Background}"
                            CornerRadius="{TemplateBinding CornerRadius}"
                            Padding="{TemplateBinding Padding}"
                            RenderTransformOrigin="0.5,0.5">
                        <Border.RenderTransform>
                            <CompositeTransform x:Name="RootTransform" TranslateX="0" ScaleX="1" ScaleY="1"/>
                        </Border.RenderTransform>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualStateGroup.Transitions>
                                    <VisualTransition To="PointerOver" GeneratedDuration="0:0:0.16">
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="RootTransform"
                                                             Storyboard.TargetProperty="TranslateX"
                                                             To="3" Duration="0:0:0.16">
                                                <DoubleAnimation.EasingFunction>
                                                    <CubicEase EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </VisualTransition>
                                    <VisualTransition From="PointerOver" To="Normal" GeneratedDuration="0:0:0.2">
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="RootTransform"
                                                             Storyboard.TargetProperty="TranslateX"
                                                             To="0" Duration="0:0:0.2">
                                                <DoubleAnimation.EasingFunction>
                                                    <CubicEase EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </VisualTransition>
                                    <VisualTransition To="Pressed" GeneratedDuration="0:0:0.08">
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="RootTransform"
                                                             Storyboard.TargetProperty="ScaleX"
                                                             To="0.98" Duration="0:0:0.08"/>
                                            <DoubleAnimation Storyboard.TargetName="RootTransform"
                                                             Storyboard.TargetProperty="ScaleY"
                                                             To="0.98" Duration="0:0:0.08"/>
                                        </Storyboard>
                                    </VisualTransition>
                                </VisualStateGroup.Transitions>
                                <VisualState x:Name="Normal"/>
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Target="RootBorder.Background" Value="{ThemeResource SubtleFillColorSecondaryBrush}"/>
                                        <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource TextFillColorPrimaryBrush}"/>
                                        <Setter Target="RootTransform.TranslateX" Value="3"/>
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <VisualState.Setters>
                                        <Setter Target="RootBorder.Background" Value="{ThemeResource SubtleFillColorTertiaryBrush}"/>
                                        <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource TextFillColorPrimaryBrush}"/>
                                        <Setter Target="RootTransform.ScaleX" Value="0.98"/>
                                        <Setter Target="RootTransform.ScaleY" Value="0.98"/>
                                        <Setter Target="RootTransform.TranslateX" Value="1.5"/>
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Disabled">
                                    <VisualState.Setters>
                                        <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource TextFillColorDisabledBrush}"/>
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <ContentPresenter x:Name="ContentPresenter"
                                          Content="{TemplateBinding Content}"
                                          ContentTransitions="{TemplateBinding ContentTransitions}"
                                          ContentTemplate="{TemplateBinding ContentTemplate}"
                                          HorizontalAlignment="Left"
                                          VerticalAlignment="Center"
                                          FontSize="13"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Fluent 2 品类筛选导航项选中样式 (带左侧 Accent 竖指示条弹性生长、轻量半透明背景与平移微动效) -->
    <Style x:Key="CategoryItemSelectedStyle" TargetType="Button">
        <Setter Property="Background" Value="{ThemeResource SubtleFillColorSecondaryBrush}"/>
        <Setter Property="Foreground" Value="{ThemeResource TextFillColorPrimaryBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="CornerRadius" Value="6"/>
        <Setter Property="Height" Value="36"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="Padding" Value="10,0,12,0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="RootBorder" 
                            Background="{TemplateBinding Background}"
                            CornerRadius="{TemplateBinding CornerRadius}"
                            Padding="{TemplateBinding Padding}"
                            RenderTransformOrigin="0.5,0.5">
                        <Border.RenderTransform>
                            <CompositeTransform x:Name="RootTransform" TranslateX="0" ScaleX="1" ScaleY="1"/>
                        </Border.RenderTransform>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualStateGroup.Transitions>
                                    <VisualTransition To="PointerOver" GeneratedDuration="0:0:0.16">
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="RootTransform"
                                                             Storyboard.TargetProperty="TranslateX"
                                                             To="2" Duration="0:0:0.16">
                                                <DoubleAnimation.EasingFunction>
                                                    <CubicEase EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </VisualTransition>
                                    <VisualTransition From="PointerOver" To="Normal" GeneratedDuration="0:0:0.2">
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="RootTransform"
                                                             Storyboard.TargetProperty="TranslateX"
                                                             To="0" Duration="0:0:0.2">
                                                <DoubleAnimation.EasingFunction>
                                                    <CubicEase EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </VisualTransition>
                                    <VisualTransition To="Pressed" GeneratedDuration="0:0:0.08">
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="RootTransform"
                                                             Storyboard.TargetProperty="ScaleX"
                                                             To="0.98" Duration="0:0:0.08"/>
                                            <DoubleAnimation Storyboard.TargetName="RootTransform"
                                                             Storyboard.TargetProperty="ScaleY"
                                                             To="0.98" Duration="0:0:0.08"/>
                                        </Storyboard>
                                    </VisualTransition>
                                </VisualStateGroup.Transitions>
                                <VisualState x:Name="Normal"/>
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Target="RootBorder.Background" Value="{ThemeResource SubtleFillColorTertiaryBrush}"/>
                                        <Setter Target="RootTransform.TranslateX" Value="2"/>
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <VisualState.Setters>
                                        <Setter Target="RootBorder.Background" Value="{ThemeResource SubtleFillColorSecondaryBrush}"/>
                                        <Setter Target="RootTransform.ScaleX" Value="0.98"/>
                                        <Setter Target="RootTransform.ScaleY" Value="0.98"/>
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Disabled"/>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <!-- Windows 11 原生侧边栏指示条 (带弹性展开动画) -->
                            <Border Grid.Column="0" 
                                    x:Name="SelectionIndicator"
                                    Width="3" 
                                    Height="16" 
                                    CornerRadius="1.5" 
                                    Background="{ThemeResource AccentFillColorDefaultBrush}" 
                                    VerticalAlignment="Center" 
                                    Margin="0,0,8,0">
                                <Border.Transitions>
                                    <TransitionCollection>
                                        <EntranceThemeTransition FromVerticalOffset="6"/>
                                        <RepositionThemeTransition/>
                                    </TransitionCollection>
                                </Border.Transitions>
                            </Border>
                            <ContentPresenter x:Name="ContentPresenter"
                                              Grid.Column="1"
                                              Content="{TemplateBinding Content}"
                                              ContentTransitions="{TemplateBinding ContentTransitions}"
                                              ContentTemplate="{TemplateBinding ContentTemplate}"
                                              HorizontalAlignment="Left"
                                              VerticalAlignment="Center"
                                              FontWeight="SemiBold"
                                              FontSize="13"/>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- WinUI Gallery 标准分段单选标签 (Segmented Control) 样式 -->
    <Style x:Key="SegmentedRadioButtonStyle" TargetType="RadioButton">
        <Setter Property="MinHeight" Value="32"/>
        <Setter Property="Padding" Value="16,6"/>
        <Setter Property="CornerRadius" Value="6"/>
        <Setter Property="Margin" Value="2,0"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Border x:Name="ContentBorder" 
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1" 
                            CornerRadius="{TemplateBinding CornerRadius}"
                            Padding="{TemplateBinding Padding}">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CheckStates">
                                <VisualState x:Name="Checked">
                                    <VisualState.Setters>
                                        <Setter Target="ContentBorder.Background" Value="{ThemeResource LayerFillColorDefaultBrush}"/>
                                        <Setter Target="ContentBorder.BorderBrush" Value="{ThemeResource AccentFillColorDefaultBrush}"/>
                                        <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource TextFillColorPrimaryBrush}"/>
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Unchecked">
                                    <VisualState.Setters>
                                        <Setter Target="ContentBorder.Background" Value="Transparent"/>
                                        <Setter Target="ContentBorder.BorderBrush" Value="Transparent"/>
                                        <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource TextFillColorSecondaryBrush}"/>
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <ContentPresenter x:Name="ContentPresenter" 
                                          Content="{TemplateBinding Content}"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- WinUI Gallery / Windows 11 设置卡片 (SettingsCard) 样式 -->
    <Style TargetType="controls:SettingsCard">
        <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}"/>
        <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Padding" Value="16,12"/>
        <Setter Property="MinHeight" Value="64"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="IsTabStop" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="controls:SettingsCard">
                    <Border x:Name="RootBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{TemplateBinding CornerRadius}"
                            Padding="{TemplateBinding Padding}"
                            MinHeight="{TemplateBinding MinHeight}">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal"/>
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Target="RootBorder.Background" Value="{ThemeResource CardBackgroundFillColorSecondaryBrush}"/>
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <Grid ColumnSpacing="16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- 左侧 36x36 矢量图标微标 Squircle 容器 -->
                            <Border Grid.Column="0" 
                                    Width="36" Height="36" 
                                    CornerRadius="6" 
                                    Background="{ThemeResource SubtleFillColorSecondaryBrush}"
                                    Visibility="{TemplateBinding HeaderIconVisibility}"
                                    VerticalAlignment="Center">
                                <FontIcon Glyph="{TemplateBinding Glyph}" 
                                          FontSize="18" 
                                          VerticalAlignment="Center" 
                                          HorizontalAlignment="Center"
                                          Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                            </Border>

                            <!-- 标题与副标题 -->
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                                <TextBlock Text="{TemplateBinding Header}" 
                                           FontSize="14" 
                                           FontWeight="SemiBold"
                                           TextWrapping="Wrap"/>
                                <TextBlock Text="{TemplateBinding Description}" 
                                           FontSize="12" 
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" 
                                           TextWrapping="Wrap"
                                           Visibility="{TemplateBinding DescriptionVisibility}"/>
                            </StackPanel>

                            <!-- 右侧交互控件内容 -->
                            <ContentPresenter Grid.Column="2" 
                                              Content="{TemplateBinding Content}"
                                              ContentTemplate="{TemplateBinding ContentTemplate}"
                                              ContentTransitions="{TemplateBinding ContentTransitions}"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Right"/>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- WinUI Gallery / Windows 11 设置卡片分组 (SettingsGroup) 样式 -->
    <Style TargetType="controls:SettingsGroup">
        <Setter Property="Margin" Value="0,0,0,24"/>
        <Setter Property="IsTabStop" Value="False"/>
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <StackPanel Spacing="4"/>
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="controls:SettingsGroup">
                    <StackPanel Margin="{TemplateBinding Margin}" Spacing="4">
                        <TextBlock Text="{TemplateBinding Header}" 
                                   FontSize="15" 
                                   FontWeight="SemiBold" 
                                   Margin="0,0,0,2"/>
                        <TextBlock Text="{TemplateBinding Description}" 
                                   FontSize="12" 
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" 
                                   Margin="0,0,0,8"
                                   Visibility="{TemplateBinding DescriptionVisibility}"/>
                        <ItemsPresenter/>
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
